<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1" />
	<title>{{ title || 'DoodleIn' }}</title>
	<link rel="icon" href="resources/images/Icon.svg" type="image/svg+xml">
	<link rel="icon" sizes="192x192" href="resources/images/Icon.png" type="image/png">

	{{-- Vite assets --}}
	@vite(['resources/css/app.css', 'resources/js/app.js'])

	{{-- Allow pages to inject meta tags --}}
	@if ($slots.meta)
	{{{ await $slots.meta() }}}
	@endif
</head>

<body>
	@include('partials/header')

	{{-- Main slot for page content --}}
	@if ($slots.main)
	{{{ await $slots.main() }}}
	@endif

	@include('partials/footer')
</body>

</html>
